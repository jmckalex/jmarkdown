# inline test

Mid-sentence, see [[file.md]] for details — the token should remain literal because it is not on its own line.

Also at the end of a line with trailing text: leading [[file.md]] trailing.
